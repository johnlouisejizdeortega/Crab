# Copy to .env (the dev scripts auto-create one if missing)
DATABASE_URL="file:./dev.db"
JWT_SECRET="change-me-in-production"
PORT=4000
CLIENT_ORIGIN="http://localhost:5173"
# Set to "false" to disable the auto-responding simulated driver fleet
SIMULATED_FLEET="true"
